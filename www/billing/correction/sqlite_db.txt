CREATE TABLE IF NOT EXISTS `m_data` (
	`id`  INTEGER PRIMARY KEY,
	`aid`  INTEGER,
	`mydate`  INTEGER,
	`link`  INTEGER,
	`equation_category` INTEGER,
	`subdivision` text NOT NULL,
	`dtrno`	text NOT NULL,
	`cid` text NOT NULL,
	`consumer_name` text NOT NULL,
	`consumer_address` text NOT NULL,
	`consumer_category` text NOT NULL,
	`connection_type` INTEGER NOT NULL,
	`mfactor` INTEGER NOT NULL,
	`connection_load` text NOT NULL,
	`meter_no` text NOT NULL,
	`reserve_unit` INTEGER NOT NULL,
	`premeter_read_date` text NOT NULL,
	`premeter_read` text NOT NULL,
	`slab` text NOT NULL,
	`meter_rent` text NOT NULL,
	`principal_arrear` text NOT NULL,
	`arrear_surcharge` text NOT NULL,
	`adjustment` text NOT NULL,

	`billno` text,
	`status` text,
	`reading_date` text,
	`postmeter_read` text,
	`meterpic` text,
	`meterpic_binary` text,
	`unit_consumed` text,
	`unit_billed` text,
	`consumtion_day` text,
	`due_date` text,
	`energy_amount` text,
	`total_energy_charge` text,
	`fixed_charge` text,
	`electricity_duty` text,
	`current_demand` text,
	`current_surcharge` text,
	`total_arrear` text,
	`net_bill_amount` text,
	`net_bill_amount_after_duedate` text,
)